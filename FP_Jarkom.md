**NOMOR 3**

- Smith-Morra
  cd /myscripts
  chmod +x <nama file>
  ./scripts_awal.sh (update, install iptables, ping google.com, iptables forward)
- Fianchetto
  cd /myscripts
  chmod +x <nama file>
  ./scripts_nameserver.sh (nano /etc/resolv.conf sama ping google.com)
- Lucena
  cd /myscripts
  chmod +x <nama file>
  ./scripts_nameserver.sh (nano /etc/resolv.conf sama ping google.com)
  chmod +x <nama file>
   ./scripts_forwarding.sh (update, install iptables, iptables forward)
- Ponziani
  cd /myscripts
  chmod +x <nama file>
  ./scripts_nameserver.sh (nano /etc/resolv.conf sama ping google.com)
  ./scripts_dhcp_server.sh (install dhcp server, trs konfigurasinya juga)
